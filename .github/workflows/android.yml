name: Build Android APK

on:
  push:
    branches: ["main"]
    paths:
      - 'android/**'
      - 'css/**'
      - 'js/**'
      - 'pages/**'
      - '*.html'
      - '.github/workflows/android.yml'
  pull_request:
    branches: ["main"]
    paths:
      - 'android/**'
      - 'css/**'
      - 'js/**'
      - 'pages/**'
      - '*.html'
      - '.github/workflows/android.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      # setup-android points ANDROID_HOME at a fresh SDK that only has the
      # command-line tools, so the platform/build-tools must be installed
      # explicitly or AGP fails to find the build target (android-34).
      - name: Install Android SDK packages
        run: sdkmanager --install "platform-tools" "platforms;android-34" "build-tools;34.0.0"

      - name: Make gradlew executable
        run: chmod +x ./gradlew
        working-directory: android

      - name: Build debug APK
        run: ./gradlew assembleDebug --no-daemon --stacktrace
        working-directory: android

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: examzen-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
