node_modules/
dist/
.DS_Store
.env
.env.*
!.env.example
public/vendor/

# Python bytecode from the turbo proxy (running `python -m py_compile app.py`
# locally, or the container importing it, drops these next to the source).
__pycache__/
*.py[cod]


# Firestore rules are managed directly in the Firebase console, not in the repo
firestore.rules
