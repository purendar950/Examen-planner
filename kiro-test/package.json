{
  "name": "kiro-key-test",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone test service for verifying a KIRO_API_KEY works via kiro-cli headless mode",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
