{
  "// note": "Config for the separately-deployed OpenCode server (opencode-*.onrender.com). Goal: keep RAM under Render 512MB free-tier limit. Copy this to the server workspace root (OPENCODE_DIRECTORY) or point OPENCODE_CONFIG at it.",

  "// lsp": "Language servers are opencode biggest memory consumers - they index the workspace on boot and leak over time. Study AI only needs text generation, not code intelligence, so disable them all.",
  "lsp": {
    "typescript": { "disabled": true },
    "eslint": { "disabled": true },
    "gopls": { "disabled": true },
    "pyright": { "disabled": true },
    "ruby-lsp": { "disabled": true },
    "rust-analyzer": { "disabled": true },
    "clangd": { "disabled": true },
    "zls": { "disabled": true },
    "csharp": { "disabled": true },
    "elixir-ls": { "disabled": true },
    "bash-language-server": { "disabled": true }
  },

  "// formatter": "Formatters spawn extra child processes; not needed for generation.",
  "formatter": {},

  "// autoupdate": "Never let the server self-update mid-run on a tiny box.",
  "autoupdate": false
}
