# [default] extra bundles yt-dlp-ejs (the JS challenge solver scripts) required
# by modern YouTube extraction. Paired with a Deno runtime (see Dockerfile).
yt-dlp[default]>=2026.7.0
flask>=3.0.0
flask-cors>=4.0.0
requests>=2.31.0
gunicorn>=21.2.0
# Lets the backend read cookies from Firestore config/turbo (admin-managed),
# same service account the Telegram bot uses. Optional: without
# FIREBASE_SERVICE_ACCOUNT set, the backend falls back to env/file cookies.
firebase-admin>=6.5.0
# S3-compatible object storage (Backblaze B2 / Cloudflare R2) for study-material
# BODIES — keeps big notes out of Firestore's 1 MiB/doc limit. Optional: without
# the S3_* env vars the backend transparently falls back to Firestore-only.
boto3>=1.34.0
# bgutil PO-token plugin is installed from the same git clone as the POT server
# (see Dockerfile) so the plugin and server versions always match.
